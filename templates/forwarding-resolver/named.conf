// Forwarding DNS Resolver
// Provides recursive resolution only

options {
    directory "/var/cache/bind";

    listen-on { any; };
    listen-on-v6 { none; };

    recursion yes;
    allow-recursion { any; };

    forwarders {
        9.9.9.9;
    };
};
