# Vanilla BIND

This configuration represents a minimal BIND installation.

- No zones
- No forwarding
- No recursion
- No access controls

Use this to observe default BIND behavior before applying real-world roles.
