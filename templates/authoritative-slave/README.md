# Authoritative Slave

This configuration defines a BIND slave server.

- Zones are received from the master
- Local edits are ignored
- Zone files are auto-managed by BIND

Slaves should never be edited manually.
