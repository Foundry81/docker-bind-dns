services:
  slave:
    build:
      context: ../
      dockerfile: Dockerfile
    image: 100781/bind-dns
    container_name: bind-slave
    ports:
      - "53:53/tcp"
      - "53:53/udp"
    volumes:
      - ./slave:/etc/bind
    restart: unless-stopped
