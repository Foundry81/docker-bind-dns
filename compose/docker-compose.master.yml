services:
  master:
    build:
      context: ../
      dockerfile: Dockerfile
    image: 100781/bind-dns
    container_name: bind-master
    ports:
      - "53:53/tcp"
      - "53:53/udp"
    volumes:
      - ./master:/etc/bind
    restart: unless-stopped
